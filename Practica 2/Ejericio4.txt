En el primer bloque, todo el texto tendrá color verde, el mismo contiene una etiqueta de párrafo que aplica los estilos de la clase "contenido", pero sobreescribe en la etiqueta peso de la fuente de bold (que aplica la clase) a "normal" y tambien cambia el color de verde la cual es una definición global a negro la cual está definida en los estilos de la etiqueta de párrafo, luego tiene una tabla la cual en una celda tiene un etiqueta “a” la cual refencia a "http://www.google.com.ar", el cual al pasar el mouse (hover) se vuelve de color rosa, al hacerle click se vuelve rojo, por defecto tiene el color azul y también contiene un texto en una celda el texto suelto “esto es una tabla…”, lo cual toma el color verde global.
En el segundo bloque a diferencia del primero todo el texto dentro del body toman los estilos de la clase contenido, la etiqueta p en este caso se va a ver como la anterior salvo que ahora tendrá el peso de fuente bold, respecto de la tabla y el enlace se comportan igual que lo descrito antes salvo que por herencia aplican los estilos de la clase "contenido”.